<%
(function(){
<<<<<<< HEAD
    var user = session.get("LOGGED_IN_USER");
=======
    var user = require('/modules/user.js').current(session).username;//session.get("LOGGED_IN_USER");
>>>>>>> 7b77151d0d114757b90fecebbf53248c201f780b
    if(user === null) {
        response.sendRedirect('/publisher');
    } else {
        var config = require('/config/publisher.js').config(),
                sso = require('sso'),
                sessionId = session.getId(),
                encodedSAMLLogoutRequest = sso.client.getEncodedSAMLLogoutRequest(user, config.ssoConfiguration.issuer),
                relayState = '/publisher';

        %>
        <div>
           <p>You are now redirected to Stratos Identity. If the
           redirection fails, please click the post button.</p>

            <form id="logoutForm" method="post" action="<%=config.ssoConfiguration.identityProviderURL%>">
            <p>
                <input type="hidden" name="SAMLRequest"
                      value="<%= encodedSAMLLogoutRequest %>"/>
                <input type="hidden" name="RelayState" value="<%= relayState %>"/>
                 <input type="hidden" name="SSOAuthSessionID" value="<%= sessionId %>"/>
                <button type="submit">POST</button>
            </p>
            </form>
        </div>
        <script type = "text/javascript" >
                       document.forms[0].submit();
        </script>

         <%
    }
}());
%>